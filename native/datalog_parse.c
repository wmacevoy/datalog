/* ============================================================
 * datalog_parse.c — .dl file parser (stub)
 *
 * TODO: Lemon grammar for datalog syntax:
 *   fact.                       → datalog_assert
 *   rule where condition.       → define view in qjson_db
 *   when event pattern: actions → register trigger
 *   signal name: data           → datalog_signal
 *   retract pattern             → datalog_retract
 *   select pattern              → datalog_select
 *   mineralize(name)            → datalog_mineralize
 *   fossilize                   → datalog_fossilize
 *   facts: block                → bulk assert
 * ============================================================ */
